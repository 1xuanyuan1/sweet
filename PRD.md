# “和熹香堂”微信小程序产品需求文档 (PRD)

## 1. 项目背景

“和熹香堂”是一个专注于合香非遗手工的品牌。为了提升客户选购体验及简化管理员（主理人）的订单管理流程，需开发一款基于微信小程序原生语法及云开发的移动应用。

## 2. 用户角色

- **管理员 (Admin)**: 品牌主理人，负责产品数据维护、订单审核、价格调整、状态更新及财务汇总。只有被授权的管理员才能进入后台管理界面。
- **客户 (Customer)**: 通过微信授权登录，进行产品选购、订单确认及状态查看。

## 3. 核心功能模块

### 3.1 用户系统 (新增)

- **授权登录**: 用户首次进入小程序需进行微信授权登录，收集用户昵称和头像。
- **身份识别**: 系统通过数据库字段标识用户是否为管理员。
- **信息关联**: 所有订单需关联下单用户的基本信息（昵称、头像）。

### 3.2 产品管理 (管理员)

- **方子管理**:
  - 维护香的配方名称、功效、主要材料及成本单价。
  - 支持新增、编辑、删除。
- **款式管理**:
  - 维护款式名称、工费、耗材量（kg/个）及款式图片（暂缺，需预留占位）。
  - 支持新增、编辑、删除。

### 3.3 选购流程 (客户)

- **自由组合**: 客户可选择一个“方子”搭配一个“款式”。
- **数量选择**: 选择该组合的需求数量。
- **价格预估**: 系统根据公式 `(方子单价 * 款式耗材量 + 款式工费) * 数量` 自动计算总价。
- **提交意向**: 客户选完后提交订单，此时状态为“待确认”。

### 3.4 订单管理

- **客户权限**: 客户在“我的订单”列表中仅能查看自己提交的订单（基于 OpenID 过滤）。
- **管理员权限**: 管理员可查看系统内所有订单，且列表需明确展示下单人的昵称与头像信息。
- **订单审核与改价**:
  - 管理员收到新订单通知。
  - 支持修改总价（如：抹零、打折、手动输入最终价）。
  - 给客户发送确认请求。
- **状态维护 (中文显示)**:
  - 确认后价格锁定。
  - 手动更新状态：待客户确认 -> 已确认 -> 已付款 -> 制作中 -> 已发货 -> 已收货。
- **物流信息**:
  - 在“已发货”阶段，管理员录入“快递公司”与“快递单号”，并通知客户。

### 3.5 财务统计 (管理员)

- **月度汇总**:
  - 按月显示：总单数、总金额。
  - 细分统计：已收款金额、待收款金额。

## 4. 数据结构设计 (云数据库)

### 4.1 Recipes (方子)

- `_id`: string
- `name`: string (如：安神方)
- `description`: string (功效)
- `ingredients`: string[] (材料)
- `pricePerKg`: number (元/kg)

### 4.2 Styles (款式)

- `_id`: string
- `name`: string (如：中元宝)
- `laborCost`: number (工费/个)
- `materialWeight`: number (耗材量 kg/个)
- `imageUrl`: string (照片路径)

### 4.3 Users (用户 - 新增)

- `_id`: string (openid)
- `nickName`: string
- `avatarUrl`: string
- `isAdmin`: boolean (是否为管理员)
- `createdAt`: Date
- `updatedAt`: Date

### 4.4 Orders (订单)

- `_id`: string
- `_openid`: string (核心字段：微信云开发自动注入的用户唯一标识，用于关联 Users 集合的 `_id`)
- `userInfo`: Object (下单时的用户快照: { nickName, avatarUrl }) - _注意：仅作为快照，最新信息需通过 \_openid 联表查询获取_
- `recipeId`: string
- `styleId`: string
- `quantity`: number
- `originalPrice`: number (原始计算总价)
- `finalPrice`: number (管理员修改后的最终价)
- `status`: string (pending/wait_confirm/confirmed/paid/producing/shipped/received)
  - 对应中文: 待确认 / 待客户确认 / 已确认 / 已付款 / 制作中 / 已发货 / 已收货
- `expressCompany`: string
- `expressNumber`: string
- `createdAt`: Date
- `updatedAt`: Date

## 5. UI/UX 风格定义

- **风格**: 新中式、禅意、简约、高雅。
- **主色调**:
  - 品牌红: `#8B0000` (深红)
  - 点缀金: `#D4AF37` (古铜金)
  - 背景色: `#F9F6F2` (宣纸色)
- **交互**: 简洁的卡片式布局，平滑的过渡动画。

## 6. 开发计划 (第一期)

- 不接入微信支付，仅进行价格确认与状态管理。
- 重点在于方子与款式的灵活组合逻辑。
- 实现基本的模板消息通知（或订阅消息）。
